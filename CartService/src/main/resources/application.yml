spring:
    application:
        name: CartService

    datasource:
      url: jdbc:mysql://localhost:3306/cart-service?useUnicode=true&useLegacyDatetimeCode=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=true
      username: root
      password: root
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true

    devtools:
      restart:
        enabled: true

    security:
      user:
        name: brajesh
        password: brajesh

server:
    port: 8082

